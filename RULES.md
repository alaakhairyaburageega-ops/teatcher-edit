# Project Rules & Constitution (RULES.md)

هذا الملف هو المرجع الأساسي الذي يجب الالتزام به في كل محادثة، وكل عملية تحليل، وكل عملية بناء، وكل تعديل، وكل إصلاح، وكل إضافة مستقبلية داخل المشروع. يجب قراءة هذا الملف والالتزام به قبل تنفيذ أي طلب أو تعديل أو كتابة كود.

## 1. إنشاء الملفات
في Root المشروع توجد الملفات التالية:
- `RULES.md`
- `CHANGELOG.md`
- `BUGS.md`
هذه الملفات موجودة بالفعل، لا تحذف محتواها ولا تستبدلها بالكامل. قم بمراجعتها وتحديثها فقط عند الحاجة.

## 2. قاعدة عدم الانحراف عن المطلوب
**القاعدة الأهم:** نفّذ المطلوب فقط. لا تضف Features أو تغييرات أو تحسينات لم يطلبها المستخدم صراحة.
لا تقم تلقائيًا بـ:
- إعادة تصميم أجزاء غير مرتبطة بالمطلوب.
- تغيير Architecture.
- تغيير المكتبات.
- تغيير Database Schema.
- تغيير API.
- إعادة كتابة Components تعمل بالفعل.
- إضافة Features جديدة.
- حذف Features موجودة.
- تغيير UI خارج نطاق الطلب.
- Refactoring واسع لمجرد أنك ترى أنه أفضل.
إذا وجدت فرصة لتحسين شيء غير مطلوب، لا تنفذه تلقائيًا. قم بتسجيله كملاحظة واقتراح فقط، وانتظر موافقة المستخدم.

## 3. افهم قبل أن تعدّل
قبل أي تعديل:
- افهم الطلب.
- افحص الملفات المرتبطة بالطلب.
- افهم Architecture الحالية.
- افهم Dependencies الموجودة.
- افهم طريقة إدارة State.
- افهم API/Database إن كانت مرتبطة.
- حدد أقل عدد ممكن من الملفات التي تحتاج إلى تعديل.
- لا تعدّل ملفات غير ضرورية.
- لا تبدأ بكتابة الكود قبل فهم السياق الموجود.

## 4. قاعدة Minimal Change
عند تنفيذ أي طلب: استخدم أقل تغيير ممكن لتحقيق المطلوب.
- لا تعيد كتابة ملف كامل إذا كان المطلوب تعديل بضعة أسطر فقط.
- لا تقم بـ Refactoring لملفات غير مرتبطة بالمشكلة.
- لا تغير Naming أو Architecture الموجودة إلا إذا كان ذلك ضروريًا لتنفيذ الطلب أو طلبه المستخدم.

## 5. التعديلات يجب أن تسجل في CHANGELOG.md
بعد كل عملية تعديل ناجحة، يجب تحديث `CHANGELOG.md` بالصيغة المطلوبة (التاريخ، الوصف، السبب، الملفات، الاختبارات). لا تسجل تغييرات وهمية.

## 6. كل خطأ يجب أن يسجل في BUGS.md
عند اكتشاف أي Bug أو Error أو Build Failure أو Runtime Error أو مشكلة في Logic، يجب تسجيله في `BUGS.md` مع ذكر الـ Root Cause والحل وسبب اختيار الحل. لا تكتفِ بكتابة أن الخطأ تم إصلاحه.

## 7. لا تخفي الأخطاء
إذا فشل Build، Test، TypeScript، Linter، API، Database، Runtime، Deployment: لا تتجاهل الخطأ ولا تعتبر المهمة مكتملة. قم بتشخيصه وإصلاحه، وسجل المشكلة والحل. إذا لم تستطع، اتركها Open.

## 8. Clean Code Principles
اكتب الكود وفق مبادئ Clean Code. استخدم أسماء واضحة ومعبرة تعبر عن المعنى، وتجنب الأسماء المبهمة مثل `x` أو `data2`.

## 9. Single Responsibility
كل Function وComponent وClass وModule يجب أن يكون له مسؤولية واضحة ومحددة. قسّم المسؤوليات بشكل منطقي، لكن لا تبالغ في تقسيم الكود بلا حاجة. الهدف الوضوح.

## 10. Functions
اجعل Functions صغيرة قدر الإمكان، واضحة، ذات مسؤولية واحدة، ذات Parameters منطقية، وبدون Side Effects غير متوقعة.

## 11. DRY (Don't Repeat Yourself)
لا تكرر نفس Logic. استخرجه إلى Utility/Helper/Component. لكن: Prefer simple abstraction over premature abstraction.

## 12. KISS (Keep It Simple, Stupid)
استخدم أبسط حل صحيح. لا تستخدم Architecture معقدة، Design Patterns، Abstractions زائدة أو حلول ذكية يصعب فهمها. الكود الجيد يفهمه مطور آخر بسهولة.

## 13. YAGNI (You Aren't Gonna Need It)
لا تبنِ شيئًا لم يُطلب (Features، APIs، Tables، Components، Configurations).

## 14. Comments
لا تكتب Comments تشرح الكود الواضح (What). التعليق يجب أن يشرح (Why): Business Rule معقد، قرار تقني غير بديهي، Workaround.

## 15. Error Handling
تعامل مع الأخطاء بشكل واضح ومفيد (للمستخدم وللمطور). لا تخفي الأخطاء باستخدام `try/catch` فارغ أو رسائل غامضة.

## 16. Type Safety
استخدم TypeScript بشكل صحيح، وتجنب `any` إلا عند الضرورة القصوى. استخدم Interfaces وTypes وType Guards. أصلح Root Cause بدل إسكات الخطأ.

## 17. Security
لا تضع Secrets وTokens داخل الكود، استخدم Environment Variables. لا تعرض معلومات حساسة في Logs. تحقق من User Input.

## 18. Dependencies
لا تضف Package جديدة إلا إذا كانت هناك حاجة حقيقية. تحقق مما إذا كانت الوظيفة يمكن تنفيذها بالموجود.

## 19. UI/UX
حافظ على Design System، Typography، Spacing، Colors وComponents الحالية. لا تعيد تصميم أجزاء غير مطلوبة. الواجهة يجب أن تكون Responsive، Accessible وسهلة الاستخدام.

## 20. Database وAPI
لا تغير Schema أو API Contract إلا إذا لزم الأمر. تحقق من تأثير أي تغيير على النظام بأكمله للحفاظ على الـ Features الحالية.

## 21. Backward Compatibility
حاول الحفاظ على الـ Features الحالية عند إضافة Feature جديدة. تحقق أن التعديل لم يكسر UI، API، Auth، أو Database.

## 22. Testing
كل تعديل يجب أن يتم التحقق منه بالمستوى المناسب (Type check, Lint, Unit, Integration, Manual). لا تقل "تم الإصلاح" قبل التحقق.

## 23. لا تعدل شيئًا خارج Scope
نفذ المطلوب فقط (مثلًا: زر جديد يعني زر جديد، وليس إعادة تصميم الصفحة بأكملها).

## 24. عند وجود غموض
إذا كان الطلب غير واضح: لا تفترض، اسأل سؤالًا توضيحيًا. إذا كان الغموض بسيطًا استخدم الحل الأقل تأثيرًا.

## 25. لا تكسر الموجود
أي Feature كانت تعمل قبل التعديل يجب أن تستمر في العمل بعده، ما لم يُطلب عكس ذلك.

## 26. قبل إنهاء أي مهمة
نفذ Checklist داخلي: المطلوب فقط؟ فهم الكود؟ أقل عدد ملفات؟ لم نكسر موجود؟ هل يوجد Error؟ Build/Test/Lint ناجح؟ تحديث CHANGELOG و BUGS؟ كود نظيف؟ لا يوجد dependencies بلا ضرورة؟

## 27. Git Safety
لا تقم بعمليات destructive (حذف Branches, Reset, Force Push) إلا بطلب صريح. لا تعتبر Git history وسيلة لتنظيف أخطاءك بدل إصلاحها.

## 28. قاعدة الأولوية
الطلب الأصلي > التحسين. الحل الصحيح والنظيف > السرعة. الحفاظ على الموجود > ميزة جديدة (ما لم يُطلب).

## 29. قاعدة عدم الادعاء
ممنوع الادعاء بنجاح Test/Build/Bug fix دون التأكد الفعلي. كن شفافًا ودقيقًا دائمًا.

## 30. بروتوكول تنفيذ كل طلب
1. **Analyze:** افهم المطلوب وحدد Scope.
2. **Inspect:** افحص الكود والملفات.
3. **Plan:** حدد أقل تغييرات لازمة.
4. **Implement:** نفذ المطلوب فقط.
5. **Verify:** شغّل الاختبارات وBuild.
6. **Fix:** أصلح أي Root Cause لمشكلة ظهرت.
7. **Document:** حدّث CHANGELOG و BUGS.
8. **Final Review:** مراجعة نهائية لجودة الكود ومطابقة المطلوب.

## 31. قاعدة إلزامية للمحادثات المستقبلية
هذا الملف هو `Persistent Project Constitution`. في بداية كل مهمة: اقرأ `RULES.md` والأجزاء ذات الصلة من `CHANGELOG.md` و `BUGS.md`. التزم بالقواعد ولا تتجاوزها إلا بطلب صريح. إذا تعارض طلب مع القواعد، وضّح التعارض أولاً.

## 32. الهدف النهائي
الهدف ليس كتابة أكبر قدر من الكود.
الهدف هو: **أقل كود ممكن + أوضح كود ممكن + أنظف Architecture ممكنة + أقل تغييرات ممكنة + أعلى استقرار ممكن.**
تعامل مع المشروع كمنتج Production حقيقي، وليس Prototype مؤقت.
