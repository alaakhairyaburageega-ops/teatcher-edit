# البنية المعمارية والأكواد البرمجية (Code Architecture)

يشرح هذا الملف البنية البرمجية، التقنيات المستخدمة، وكيفية ترابط ملفات الواجهة الأمامية (Frontend) وإدارة الحالة (State Management) لمشروع **SpokenEnglish ERP**.

## 1. التقنيات المستخدمة (Tech Stack)
تم بناء الواجهات الأمامية باستخدام تقنيات خفيفة وسريعة (Vanilla Stack) لضمان أقصى درجات الأداء:
- **HTML5 & CSS3:** لبناء الهيكل وتنسيق الواجهات، مع استخدام CSS Variables للتحكم بالثيمات (Themes) بشكل ديناميكي ومباشر.
- **Alpine.js:** إطار عمل جافاسكريبت خفيف جداً يُستخدم لإدارة الحالة (State Management) والتفاعل اللحظي (Reactivity) في الواجهة بدون الحاجة لمكاتب معقدة مثل React.
- **Chart.js:** مكتبة جافاسكريبت قوية ومفتوحة المصدر لرسم المخططات الإحصائية (Charts).
- **Vanilla JavaScript (ES6+):** للتعامل مع المنطق البرمجي، الـ Fetch API، والهياكل المخصصة (مثل الجدول).

## 2. هيكلة الملفات الرئيسية (Directory Structure)

يتم تنظيم المشروع كالتالي (كمثال لمجلد المعلم `teacher`):
- `teacher/index.html`: الملف الرئيسي الذي يحتوي على هيكل HTML ويربط جميع مكتبات الـ JS والـ CSS، ويعرّف المكون الأساسي للـ Alpine.js.
- `teacher/css/teacher.css`: ملف التنسيق المخصص للوحة المعلم، يحتوي على متغيرات الألوان، وقواعد العرض (Media Queries)، وتنسيقات الوضع الفاتح (`.light-mode`).
- `teacher/js/teacher-app.js`: العقل المدبر للوحة المعلم، يحتوي على منطق الـ Alpine.js ومعالجة البيانات.
- `shared/js/attendance-grid.js`: كلاس جافاسكريبت مخصص (Vanilla JS Class) يستخدم لإنشاء جدول (Matrix) معقد ومتقدم بعيداً عن قيود Alpine، ويتم استدعاؤه متى دعت الحاجة.

## 3. شرح الأكواد البرمجية والفنكشنز (Functions & Logic)

### أ. إدارة الحالة باستخدام Alpine.js (`teacher-app.js`)
يعتمد النظام على `Alpine.data('teacherApp', () => ({ ... }))` والذي يخزن المتغيرات والدوال المشتركة للملف بالكامل:
- **المتغيرات (State):** 
  - `isLightMode`: متغير منطقي (Boolean) يتحكم في مظهر الواجهة.
  - `viewMode`: متغير نصي (String) يحدد الشاشة النشطة حالياً (مثل 'analytics', 'matrix').
  - `students`, `classes`: مصفوفات لتخزين بيانات الطلاب والفصول.
- **الاستماع للتغيرات (Watchers):** يتم استخدام `this.$watch` لمراقبة متغير `isLightMode`، بحيث عندما يتم التبديل للوضع الفاتح، يتم إعادة استدعاء دالة `initAnalytics()` لإعادة رسم المخططات بألوان مناسبة.

### ب. منطق الرسوم البيانية (`initAnalytics`)
تستخدم دالة `initAnalytics()` في ملف `teacher-app.js` لإنشاء رسوم `Chart.js`:
- تقوم الدالة أولاً بتدمير (`destroy`) أي رسم بياني موجود مسبقاً لتفادي تكرار الرسم.
- تتحقق الدالة من متغير `this.isLightMode` لاختيار الألوان المناسبة (ألوان داكنة للوضع الفاتح، وألوان نيون للوضع المظلم).
- يتم رسم `attendanceChart` كخط زمني (Line Chart)، ورسم `sttChart` كدائرة (Doughnut Chart).

### ج. كلاس شبكة الحضور (`attendance-grid.js`)
هذا الملف عبارة عن Class مستقل، تم تصميمه كـ Component منفصل:
- **لماذا لم نستخدم Alpine.js هنا؟** لأن شبكة (Matrix) تولد مئات الخلايا المعقدة، واستخدام Vanilla JS يضمن سرعة وأداء أعلى بكثير في هذه الحالة المعينة.
- **التصميم المستقل:** الكلاس يمتلك الستايلات (`<style>`) الخاصة به والمدمجة في الكود (Inline injected styles)، وقد تم تحديثها مؤخراً لتدعم متغيرات الثيمات (CSS Variables).

## 4. الباك إند والبيانات الوهمية (Mock Backend & API)

بما أن هذا المشروع يُختبر كواجهة مستخدم (MVP) بدون سيرفر باك إند نشط (Live Backend):
- **عملية الجلب (Fetch API):** تم بناء الكود ليحاول أولاً جلب البيانات عبر API حقيقي باستخدام `fetch('/.netlify/functions/erp-api')`.
- **التقاط الخطأ (Error Catching):** في حال كان السيرفر غير موجود (عند تشغيل المشروع محلياً عبر Live Server)، يتم التقاط الخطأ في الـ `catch(err)`.
- **البيانات الوهمية (Mock Data Fallback):** بدلاً من إظهار رسالة خطأ للمستخدم (Network Error)، يقوم كود الجافاسكريبت تلقائياً بضخ (Inject) بيانات وهمية (Mock Data) كبديل للرد القادم من الـ API، مما يضمن ظهور الجداول والبطاقات لغرض التصميم والعرض واختبار الـ UI.
