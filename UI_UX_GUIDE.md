# دليل واجهة المستخدم وتجربة المستخدم (UI/UX Guide)

يقدم هذا الملف دليلاً شاملاً لشرح واجهات لوحة تحكم المعلم (Teacher Portal)، ووظائف الأزرار، وتجربة المستخدم (UX) في مشروع **SpokenEnglish ERP**.

## 1. الهوية البصرية والتصميم (Theme & Styling)
تم تصميم الواجهة لتكون **عصرية (Modern)** واحترافية (Premium) وتعتمد على الطابع الزجاجي (Glassmorphism):
- **الوضع الداكن (Dark Mode):** وهو الوضع الافتراضي، يتميز بألوان نيون (Neon) وألوان داكنة مريحة للعين، مع تدرجات لونية (Gradients) للرسوم البيانية والعناصر الجمالية.
- **الوضع الفاتح (Light Mode):** تم تصميمه كبديل احترافي ونظيف، حيث يعتمد على خلفيات بيضاء ورمادية فاتحة (Off-white) مع نصوص داكنة، مع تغيير ألوان المخططات لتناسب الشاشات المضيئة. يمكن التبديل بينهما عبر زر (Toggle Theme) في الشريط العلوي.

## 2. الهيكل الرئيسي للواجهة (Layout Structure)
تتكون الواجهة من جزأين رئيسيين:
1. **القائمة الجانبية (Sidebar):** تحتوي على شعار المنظمة (Logo)، أزرار التنقل بين الصفحات الأساسية، وزر تسجيل الخروج.
2. **منطقة العرض الرئيسية (Main Dashboard):** تتغير محتوياتها بناءً على التبويب المختار، وتحتوي على شريط علوي يضم معلومات المعلم وزر الوضع المظلم/الفاتح.

## 3. التبويبات والشاشات الأساسية (Views)

### أ. الإحصائيات (Analytics) - الشاشة الافتراضية
تعرض هذه الشاشة لوحات معلومات (Dashboards) سريعة:
- **مؤشر الحضور (Attendance Trend):** رسم بياني خطي يعرض نسبة الحضور للطلاب في الأيام السابقة.
- **معدل حديث الطالب (STT):** رسم بياني دائري (Doughnut Chart) يقارن بين وقت تحدث المعلم ووقت تحدث الطلاب لتقييم التفاعل.

### ب. إدخال الحضور اليومي (Daily Input)
شاشة مخصصة لتسجيل الحضور السريع اليومي للطلاب في الفصل:
- **بطاقات الطلاب (Student Cards):** يعرض كل طالب في بطاقة منفصلة مع صورته، اسمه، وحالته (Remote / In-Person).
- **أزرار التسجيل السريع:** يمكن للمعلم النقر على الأزرار لتغيير حالة الطالب بين (حاضر ✔️، غائب ❌، متأخر).
- زر **Save Attendance:** لحفظ البيانات.

### ج. شبكة الحضور (Matrix View)
شاشة متقدمة تعرض جدولاً زمنياً للحضور:
- **الجدول الزمني:** يعرض تواريخ الحصص كأعمدة، وأسماء الطلاب كصفوف، مما يسهل رؤية نمط غياب الطالب خلال الشهر.
- **الرؤوس المثبتة (Sticky Headers):** يبقى اسم الطالب وأعمدة الإجماليات ظاهرة عند التمرير الجانبي.
- **البيانات الوهمية:** في حال عدم توفر خادم (Backend)، تم تهيئة الجدول لعرض بيانات وهمية لضمان عمل الواجهة للعرض المبدئي (MVP).

## 4. وظائف الأزرار وعناصر التحكم (Controls & Buttons)
- **زر (Light Mode / Dark Mode):** يغير ألوان وتصميم النظام بالكامل بدون إعادة تحميل الصفحة.
- **القوائم المنسدلة (Dropdowns):** مثل اختيار "الفصل (Class Group)" واختيار "التاريخ (Date)".
- **أزرار (+ GROUP) و (+ STUDENT):** تفتح نوافذ منبثقة (Modals) لإضافة بيانات جديدة دون مغادرة الصفحة.
- **زر (HUB):** زر اختصار قد يُستخدم للانتقال السريع للإعدادات أو الرسائل.

## 5. النوافذ المنبثقة (Modals)
تستخدم النوافذ المنبثقة (Popups) للحفاظ على تركيز المستخدم:
- **نافذة تفاصيل الطالب (Notes):** عند النقر على أيقونة الملاحظات بجانب اسم الطالب.
- **نافذة تواصل (Contact Options):** عند النقر على أيقونة الهاتف، تظهر خيارات الاتصال بالطالب أو ولي أمره (اتصال، واتساب، الخ).
