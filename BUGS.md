# Bugs Tracker

سجل بجميع الأخطاء (Bugs, Errors, Build Failures, Runtime Errors) في المشروع.

## قالب التسجيل:

```markdown
## BUG-001 — عنوان المشكلة

### الحالة
Resolved / Open

### المشكلة
وصف واضح للمشكلة.

### Root Cause
السبب الحقيقي للمشكلة.

### الملفات المتأثرة
- `path/to/file`

### الحل
شرح الحل الذي تم تطبيقه.

### سبب اختيار الحل
لماذا هذا الحل أفضل من البدائل.

### التحقق
كيف تم التأكد أن المشكلة تم حلها.

### ملاحظات
أي تفاصيل إضافية.
```

## BUG-001 — زر Light Mode لا يعمل (تنسيقات لا تظهر)

### الحالة
Resolved

### المشكلة
زر التبديل يعمل برمجياً، والنص يتغير إلى "Dark Mode"، ولكن الخلفية والألوان لا تتغير إلى الوضع الفاتح إطلاقاً على شاشات الكمبيوتر.

### Root Cause
أثناء إضافة الكود الخاص بـ `.light-mode` في ملف `teacher.css`، تم عن طريق الخطأ حذف قوس الإغلاق `}` الخاص بـ `@media (max-width: 768px)`. أدى هذا إلى حصر جميع قواعد الـ Light Mode داخل استعلام الشاشات الصغيرة، مما جعلها غير نشطة على شاشات سطح المكتب.

### الملفات المتأثرة
- `Frontend_UI_Task_For_Engineer/03.1_SpokenEnglish ERP/teacher/css/teacher.css`

### الحل
إضافة قوس الإغلاق `}` الناقص قبل بداية تعريف قواعد `.light-mode`.

### سبب اختيار الحل
هذا هو الحل الوحيد الصحيح لإصلاح خطأ قواعد الـ CSS (Syntax Error).

### التحقق
تم مراجعة بناء الجملة (Syntax) والتأكد أن الكلاسات الآن تعمل في النطاق العام (Global Scope) وتُطبق مباشرة بمجرد أن يقوم الزر بتفعيل كلاس `light-mode` على وسم الـ `body`.

## BUG-002 — خطأ في تحميل بيانات شبكة الحضور (Network error loading matrix)

### الحالة
Resolved

### المشكلة
عند الضغط على "MATRIX VIEW" في لوحة المعلم، تظهر رسالة خطأ باللون الأحمر "Network error loading matrix" ولا يظهر الجدول.

### Root Cause
تطبيق واجهة المعلم (Teacher App) كان يحاول جلب البيانات الفعلية من خادم خلفي (Backend API) غير موجود حالياً (لأن المشروع يتم تشغيله محلياً عبر Live Server بدون خادم Netlify حقيقي). بالتالي، فشل طلب الجلب (Fetch Request) وأدى إلى عرض رسالة الخطأ.

### الملفات المتأثرة
- `Frontend_UI_Task_For_Engineer/03.1_SpokenEnglish ERP/teacher/js/teacher-app.js`

### الحل
تم تعديل دالة `loadMatrixData()` لالتقاط الخطأ عند فشل الجلب (catch block) وتوليد **بيانات وهمية (Mock Data)** تحاكي البيانات الحقيقية من الخادم (مثل إضافة طلاب وجلسات افتراضية).

### سبب اختيار الحل
هذا الحل يسمح بتجربة واختبار واجهة المستخدم (UI) والجدول (Matrix) بالكامل دون الحاجة إلى تشغيل خادم خلفي، مما يتناسب مع هدف تسليم الواجهة كـ (MVP).

### التحقق
تم التأكد برمجياً أنه عند حدوث الخطأ يتم استخدام المصفوفات الوهمية لتعبئة الجدول بنجاح.
