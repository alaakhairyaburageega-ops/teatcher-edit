# Project Rules & Constitution (RULES.md)

هذا الملف هو المرجع الأساسي الذي يجب الالتزام به في كل محادثة، وكل عملية تحليل، وكل عملية بناء، وكل تعديل، وكل إصلاح، وكل إضافة مستقبلية داخل المشروع. يجب قراءة هذا الملف والالتزام به قبل تنفيذ أي طلب أو تعديل أو كتابة كود.
هذه القواعد مبنية على توجيهات مدير المشروع وملف `README_FOR_ENGINEER.md`.

## 1. إنشاء الملفات
في مسار الـ Root للمشروع توجد الملفات التالية:
- `RULES.md` (قواعد المشروع)
- `CHANGELOG.md` (سجل التغييرات)
- `ADR.md` (سجل القرارات التقنية - Architecture Decision Records)
- `BUGS.md` (سجل الأخطاء)
هذه الملفات موجودة بالفعل، لا تحذف محتواها ولا تستبدلها بالكامل. قم بمراجعتها وتحديثها فقط عند الحاجة.

## 2. النطاق الأساسي والتقنيات (Project Scope & Tech Stack)
- **الالتزام الحرفي:** ركز فقط على تطوير وتحسين واجهات المستخدم (UI/UX) كما هو مطلوب في `README_FOR_ENGINEER.md`.
- **التقنيات المستخدمة:** استخدم تقنيات (Vanilla HTML, CSS, JavaScript) للحفاظ على البساطة المطلوبة من مدير المشروع. لا تقم بدمج مكتبات خارجية معقدة.
- **لا للـ Backend:** لا تحاول إصلاح أو الاتصال بـ APIs. استخدم بيانات وهمية (Mock Data) وتجاهل أي Fetch/API Errors تظهر في الـ Console.
- **التجاوب واللغة:** الواجهات يجب أن تكون متجاوبة (Responsive) مع كافة الأجهزة. ضع في الاعتبار دعم اللغة العربية (RTL) كميزة إضافية إذا أمكن.

## 3. قاعدة عدم الانحراف عن المطلوب
**القاعدة الأهم:** نفّذ المطلوب فقط. لا تضف Features أو تغييرات أو تحسينات لم يطلبها المستخدم صراحة.
لا تقم تلقائيًا بـ:
- إعادة تصميم أجزاء غير مرتبطة بالمطلوب.
- تغيير Architecture أو المكتبات أو Database Schema أو API.
- إعادة كتابة Components تعمل بالفعل.
- إضافة/حذف Features.
- تغيير UI خارج نطاق الطلب أو Refactoring واسع لمجرد أنك ترى أنه أفضل.
إذا وجدت فرصة لتحسين شيء غير مطلوب، لا تنفذه تلقائيًا. قم بتسجيله كملاحظة واقتراح فقط، وانتظر موافقة المستخدم.

## 4. افهم قبل أن تعدّل
قبل أي تعديل:
- افهم الطلب، وافحص الملفات المرتبطة به.
- افهم Architecture و Dependencies الحالية و طريقة إدارة State.
- حدد أقل عدد ممكن من الملفات التي تحتاج إلى تعديل (لا تعدل ملفات غير ضرورية).
- لا تبدأ بكتابة الكود قبل فهم السياق الموجود.

## 5. قاعدة Minimal Change
عند تنفيذ أي طلب: استخدم أقل تغيير ممكن لتحقيق المطلوب.
- لا تعيد كتابة ملف كامل إذا كان المطلوب تعديل بضعة أسطر فقط.
- لا تقم بـ Refactoring لملفات غير مرتبطة بالمشكلة.
- لا تغير Naming أو Architecture الموجودة إلا للضرورة أو بطلب صريح.

## 6. التوثيق (CHANGELOG & ADR)
- **CHANGELOG:** بعد كل عملية تعديل ناجحة، يجب تحديث `CHANGELOG.md` بالصيغة المطلوبة (التاريخ، الوصف، السبب، الملفات، الاختبارات). لا تسجل تغييرات وهمية.
- **ADR:** أي قرار تقني أو هيكلي يجب تسجيله أولاً في `ADR.md`.

## 7. تسجيل وإصلاح الأخطاء (BUGS)
- كل خطأ يكتشف (Bug, Error, Build/Runtime Failure) يجب أن يسجل في `BUGS.md` مع ذكر الـ Root Cause والحل وسبب اختيار الحل.
- لا تخفي الأخطاء (إذا فشل Test أو API أو Linter): لا تتجاهل الخطأ ولا تعتبر المهمة مكتملة. قم بتشخيصه وإصلاحه، وسجل المشكلة والحل. إذا لم تستطع، اتركها Open.

## 8. مبادئ الكود النظيف (Clean Code Principles)
- اكتب الكود وفق مبادئ Clean Code. استخدم أسماء واضحة ومعبرة، وتجنب الأسماء المبهمة مثل `x` أو `data2`.
- **Single Responsibility:** كل Function وComponent يجب أن يكون له مسؤولية واضحة ومحددة. قسّم المسؤوليات بشكل منطقي دون مبالغة.
- **Functions:** اجعلها صغيرة، واضحة، بمسؤولية واحدة، وبدون Side Effects غير متوقعة، وتجنب التداخل والتعقيد (Deep Nesting).
- **مكونات قابلة لإعادة الاستخدام (Reusable Components):** ابنِ مكونات مثل (Tables, Buttons, Modals) لتكون قابلة لإعادة الاستخدام.
- **DRY (Don't Repeat Yourself):** لا تكرر نفس Logic، استخرجه إلى Utility/Component، لكن Prefer simple abstraction over premature abstraction.
- **KISS (Keep It Simple, Stupid):** استخدم أبسط حل صحيح وواضح. الكود الجيد يفهمه مطور آخر بسهولة.
- **YAGNI (You Aren't Gonna Need It):** لا تبنِ شيئًا لم يُطلب.

## 9. التعليقات (Comments)
لا تكتب Comments تشرح الكود الواضح (What). التعليق يجب أن يشرح (Why): قاعدة عمل معقدة، قرار تقني غير بديهي، Workaround.

## 10. معالجة الأخطاء والأمان (Error Handling & Security)
- **Error Handling:** تعامل مع الأخطاء بشكل مفيد. لا تخفي الأخطاء باستخدام `try/catch` فارغ أو رسائل غامضة.
- **Security:** لا تضع Secrets/Tokens داخل الكود (استخدم Env Vars). تحقق من User Input.
- **Type Safety:** (إن وجد TypeScript) استخدمه بشكل صحيح وتجنب `any`. أصلح الـ Root Cause بدل إسكات الخطأ.

## 11. واجهة المستخدم (UI/UX) & Dependencies
- **Dependencies:** لا تضف Package جديدة إلا إذا كانت هناك حاجة حقيقية (ومسموح بها في المشروع).
- **UI/UX:** حافظ على Design System، Typography، Spacing، و Colors الحالية. لا تعيد تصميم أجزاء غير مطلوبة. الواجهة يجب أن تكون متجاوبة وسهلة الاستخدام.

## 12. الحفاظ على الاستقرار (Backward Compatibility)
- لا تغير Schema أو API Contract إلا إذا لزم الأمر.
- حافظ على الـ Features الحالية عند إضافة Feature جديدة. تحقق أن التعديل لم يكسر UI، API أو قاعدة البيانات.
- أي Feature كانت تعمل قبل التعديل يجب أن تستمر في العمل بعده، ما لم يُطلب عكس ذلك (لا تكسر الموجود).

## 13. الاختبار والتحقق (Testing & Verification)
- كل تعديل يجب أن يتم التحقق منه بالمستوى المناسب. لا تقل "تم الإصلاح" قبل التحقق.
- **لا تدعّي:** ممنوع الادعاء بنجاح Test/Build/Bug fix دون التأكد الفعلي. كن شفافًا ودقيقًا دائمًا.

## 14. عند وجود غموض
إذا كان الطلب غير واضح: لا تفترض، اسأل سؤالًا توضيحيًا. إذا كان الغموض بسيطًا استخدم الحل الأقل تأثيرًا.

## 15. التشخيص قبل الإنهاء (Checklist)
قبل إنهاء أي مهمة، نفذ Checklist داخلي: المطلوب فقط؟ فهم الكود؟ أقل عدد ملفات؟ لم نكسر موجود؟ هل يوجد Error؟ تحديث CHANGELOG و BUGS و ADR؟ كود نظيف؟

## 16. بروتوكول تنفيذ كل طلب
1. **Analyze:** افهم المطلوب وحدد Scope.
2. **Inspect:** افحص الكود والملفات.
3. **Plan:** حدد أقل تغييرات لازمة.
4. **Implement:** نفذ المطلوب فقط.
5. **Verify:** شغّل الاختبارات وتحقق.
6. **Fix:** أصلح أي Root Cause لمشكلة ظهرت.
7. **Document:** حدّث CHANGELOG و BUGS و ADR.
8. **Final Review:** مراجعة نهائية لجودة الكود ومطابقة المطلوب.

## 17. سلامة الـ Git (Git Safety)
لا تقم بعمليات مدمرة (حذف Branches, Reset, Force Push) إلا بطلب صريح. لا تعتبر مسار الـ Git وسيلة لتنظيف أخطاءك بدل إصلاحها.

## 18. قاعدة الأولوية
الطلب الأصلي > التحسين. الحل الصحيح والنظيف > السرعة. الحفاظ على الموجود > ميزة جديدة (ما لم يُطلب).

## 19. الهدف النهائي والبروتوكول المستقبلي
- هذا الملف هو `Persistent Project Constitution`. في بداية كل مهمة، التزم بالقواعد ولا تتجاوزها إلا بطلب صريح. إذا تعارض طلب مع القواعد، وضّح التعارض أولاً.
- الهدف ليس كتابة أكبر قدر من الكود، بل: **أقل كود ممكن + أوضح كود ممكن + أنظف Architecture ممكنة + أقل تغييرات ممكنة + أعلى استقرار ممكن.** تعامل مع المشروع كمنتج Production حقيقي.
